# NutJob — Progress vs Requirements

> Last updated: 2026-08-31 (MIGRATION COMPLETE — www.rootloot.ai serves from the VPS with self-hosted Supabase; login verified. Remaining: backups, apex record, secret rotation, DNS to Cloudflare. See Changelog.)

## Overall Status: Launch-Ready  <!-- launch-prep pass completed 2026-07-02 -->

---

## ✅ Tech Stack & Architecture

| Requirement | Status | Notes |
|---|---|---|
| Next.js + Tailwind CSS frontend | ✅ Done | Project scaffolded with both |
| App Router backend | ✅ Done | App Router in use |
| Hosting | 🚧 Migrating | Netlify → Hostinger VPS via Coolify. `Dockerfile` + `output: 'standalone'` added; Netlify kept as rollback until cutover. See `INFRA.md` |
| Supabase for DB + Auth | 🚧 Migrating | `utils/supabase/` client, server, middleware wired up. Moving from Supabase Cloud to self-hosted Supabase on the VPS — no app code change, three env vars |
| LLM | ✅ Done | **OpenRouter**, called directly. The previous "Netlify AI Gateway" row was inaccurate — `NETLIFY_AI_GATEWAY_URL` was never read by any code |
| Auth middleware (route protection) | ✅ Done | `proxy.ts` (Next 16's name for middleware) guards dashboard routes |

---

## 📄 Pages — 8 Required

### 1. Login Page — ✅ 100% Done
- Email + password form built (`app/(auth)/login/page.tsx`)
- Supabase auth action wired (`actions.ts`)
- Sign-out button component exists
- ✅ "Remember me" fully implemented with session-only/30-day cookie logic
- ✅ Role-based permissions enforced at login layout (admin / worker / supervisor)

### 2. Dashboard — ✅ 100% Done
All 6 dashboard components exist under `app/components/dashboard/`:
- `KPIGrid.tsx` — four top metrics ✅ (dynamic average moisture, Open-Meteo total rain forecast, unresolved alerts count, scheduled irrigation run time)
- `WeatherStrip.tsx` — 7-day weather strip ✅ (Open-Meteo integrated)
- `ActiveAlerts.tsx` — alerts panel ✅ (live unresolved alerts list with relative dates & domain types)
- `BlockStatusGrid.tsx` — green/amber/red health per block ✅ (live status levels based on block active alerts, moisture percentages & issues)
- `UpcomingCalendar.tsx` — calendar widget ✅ (displays upcoming calendar events from Supabase)
- `ActivityFeed.tsx` — recent activity feed ✅ (lists completed activity logs with relative times)

### 3. Blocks Page — ✅ Done (Phase 1, 2, 3 & 4)
- Route: `app/(dashboard)/blocks/page.tsx` ✅ (Server Component — fetches blocks + boundary from Supabase)
- `BlocksPage.tsx` — client shell with satellite map + block detail panel + edit-mode state machine ✅
- `BlockSatelliteMapClient.tsx` — Leaflet satellite map (Esri World Imagery, no API key) with polygon draw/edit tools via leaflet-draw ✅
- `BlockSatelliteMap.tsx` — SSR-safe `next/dynamic` wrapper (ssr: false) ✅
- `GoToLocationBar.tsx` — GPS coordinate jump input for navigating to non-adjacent farm plots in edit mode ✅
- Block boundary draw flow: Edit Farm Map → draw polygon → BlockFormModal pre-loaded with boundary → Create Block → polygon saved to DB ✅
- Static/edit mode toggle: Accept Changes persists all boundary edits to Supabase; Cancel reverts ✅
- Block list pills below map (shows all blocks; "○" marker for blocks without a boundary yet) ✅
- `BlockFormModal.tsx` — map position grid inputs removed; replaced with boundary status badge (shows point count) ✅
- `BlockDetailPanel.tsx` — tabbed panel with 5 domain tabs + alert count badges ✅
- `tabs/SoilWaterTab.tsx` — moisture bar, EC, ETo, water deficit, irrigation schedule, Lab Test History section (live Supabase fetch of manual readings, signed URL file links) ✅
- `tabs/PhenologyTab.tsx` — season timeline, growth stage, GDD, chill hours, harvest window ✅
- `tabs/NutritionTab.tsx` — nutrient gauge bars with sufficiency ranges, tissue analysis, fertigation history ✅
- `tabs/PestDiseaseTab.tsx` — risk level, observation cards, scouting schedule ✅
- `tabs/WeatherTab.tsx` — current conditions, risk flags, 24-hr forecast strip ✅
- `LogTestResultModal.tsx` — modal for logging soil/water test results ✅
- `AlertBadge.tsx` + `SourceBadge.tsx` — shared UI atoms ✅
- `types.ts` + `mockData.ts` — full type system + rich mock data for domain profiles ✅
- `app/actions/blocks.ts` — createBlock, updateBlock, deleteBlock, updateBlockBoundary server actions ✅
- **Completed step**: Run `ALTER TABLE blocks ADD COLUMN IF NOT EXISTS boundary JSONB;` in Supabase SQL editor ✅
- **Completed step**: `lab-reports` Supabase storage bucket and DB migration (adding missing columns and updating soil_water_latest view) have been successfully applied ✅

### 4. Calendar Page — ✅ Done
- Full page route `app/(dashboard)/calendar/page.tsx`
- Toggles for Month, Week (time grid), and Day (timeline) views
- Colour-coded event pills (Irrigation, Fertigation, Spraying, Pruning, Scouting, Weather Alerts)
- "Add Event" modal with activity-specific field blocks
- "Log Completion" modal to capture actual start/end and notes
- Shared types and mock data in `types.ts`
- ✅ **Inventory integration** — "Planned Materials" picker in AddEventModal lets managers assign consumables + quantities when creating tasks; stored in `calendar_event_materials` table. LogCompletionModal shows pre-filled actual quantities; on completion, inventory is automatically deducted (inserts into `consumable_usage_log`, updates `consumables.current_balance`). Migration: `supabase/migrations/20260618000000_add_calendar_event_materials.sql`.

### 5. Recommendations Page — ✅ 100% Done
- Route: `app/(dashboard)/recommendations/page.tsx` ✅
- `RecommendationsClient.tsx` — pending/history toggle, category filter, card grid ✅
- `RecommendationCard.tsx` — category icon/colour, confidence badge, accept/edit/skip controls ✅
- `actions.ts` — `getRecommendations`, `updateRecommendationStatus`, `editRecommendation`, `generateMockRecommendations` ✅
- Edit modal — manager can tweak title, add note, accept with changes (status → "edited") ✅
- Manager note shown in history card footer ✅
- Accept writes to `activity_log` (title, activity_type, block_id, rationale, performed_by, JSON details) and stores the returned `activity_log_id` on the recommendation ✅
- Edit ("Accept with changes") also writes to `activity_log` with the manager's revised title and note ✅
- AI generation (`generateAIRecommendations`) — fetches blocks + soil/weather/alerts/scouting/tissue context, calls Gemini 2.5 Flash via OpenRouter with a shared system prompt, validates and inserts results ✅ (note: not Anthropic/Claude Haiku despite earlier entries in this table — see 2026-05-26 "AI engine switched to direct OpenRouter calls")
- ✅ Priority/urgency ordering — sorted by confidence DESC (highest = most urgent) then created_at DESC as tiebreaker
- ✅ RAG grounding — generation logic consolidated into `utils/generate-recommendations.ts` (shared by the on-demand action and the weekly Trigger.dev cron); each block's synthesized query retrieves crop-matched excerpts from a `pgvector`-backed `knowledge_base_chunks` table (starting corpus: UC ANR almond production manual, ingested via `npm run ingest:kb -- --crop=<crop_type>`) and injects them as reference material; the model cites sources in a new `sources` JSONB column, shown on the recommendation card and in the edit modal

### 6. Activity Log Page — ✅ Done
- Route: `app/(dashboard)/activity/page.tsx` ✅ (Server component — parallel-fetches entries + block list from Supabase)
- `ActivityLogClient.tsx` — search by title, filter by activity type & block, live count, empty state ✅
- `actions.ts` — `getActivityLog` (search/filter/paginate), `getBlocks`, `logActivity` (manual insert via admin client) ✅
- Colour-coded activity type badges (irrigation, fertigation, spraying, pruning, scouting, tissue-sample, other) ✅
- Shows block name, performed-by, and description per entry ✅
- **"Log Activity" button** (supervisor/admin only) — opens `LogActivityModal` with activity type grid, title, block selector, datetime picker, and notes; inserts directly into `activity_log` via server action with optimistic UI prepend ✅

### 7. Inventory Page — ✅ 100% Done
- Route: `app/[farmId]/(dashboard)/inventory/page.tsx` ✅
- `InventoryPage.tsx` — asset and consumable tabs with role-based permissions (admin/supervisor edit, worker read-only) ✅
- **Asset tracking** — `AssetCard.tsx` with status badges, maintenance log history ✅
- **Consumable tracking** — `ConsumableRow.tsx` with quantity levels, low-stock alerts ✅
- CRUD modals: `AddAssetModal.tsx`, `AddConsumableModal.tsx`, `LogMaintenanceModal.tsx`, `LogUsageModal.tsx` ✅
- Calendar event usage linking — consumable usage rows link to calendar events ✅
- Searchable dropdown suggestions and low-stock alert badges ✅

### 8. Settings Page — ✅ 100% Done
- Route exists: `app/(dashboard)/settings/`
- `SettingsForms.tsx` rebuilt with 7 tabs (role-gated)
- **Account & Security tab** — profile + password update ✅
- **Team Management tab** (admin + supervisor) — role assignment & new worker invite/creation ✅
- **Block Configuration tab** (admin + supervisor) — per-block field capacity (%), wilting point (%), and notes with live bars and Supabase persistence via `updateBlockConfig` server action ✅
- **Alert Thresholds tab** (admin + supervisor) — 5 threshold sliders (soil moisture, water deficit, heat stress, rainfall skip, pest risk) stored in localStorage ✅
- **Sensor Connections tab** (admin only) — full sensor device registry: register sensors, assign to blocks (multiple sensors per block supported), copy/regenerate API keys, real-time status badges, ingest endpoint reference ✅
- **Weather & AI tab** (admin only) — farm lat/lng config with Open-Meteo live connection test + Netlify AI Gateway status ✅
- **Language tab** (all roles) — globe icon + dropdown to switch between English / العربية / Türkçe; saves to cookie + triggers full reload ✅

---

## 🗄️ Data Layer

| Requirement | Status | Notes |
|---|---|---|
| Supabase client/server setup | ✅ Done | `utils/supabase/` |
| In-field sensors (15-min updates) | ✅ Done | `sensors` table, registration UI, 3 ingest endpoints (`/api/ingest/soil|weather|alert`), and full SenseCAP pull pipeline (`/api/cron/sensecap-sync` + Trigger.dev 3×/day). Configure farm credentials in Settings → Sensors to activate live readings. |
| Weather forecast API (3-hr updates) | ✅ Done | Cron endpoint `/api/cron/weather` fetches Open-Meteo every 3 hr; stores per-farm + per-block snapshots in `weather_snapshots` |
| Manual logs (irrigation, spray, scouting, etc.) | ✅ Done | `LogActivityModal` extended with activity-specific fields (duration/volume/method for irrigation; product/amount/unit for fertigation; product/rate/target/PHI for spraying; risk/observations/next-date for scouting; type/intensity for pruning). Structured details stored in `activity_log.details` JSONB. Fertigation side-writes to `fertigation_log`; scouting side-writes to `scouting_reports` (both guarded by block_id). Detail chips rendered inline in Activity Log entries. Offline queue carries details through sync. |
| Initial block data / PDF upload & extraction | ✅ Done | AI extraction of soil/water test results from both PDF and image uploads using OpenRouter + Trigger.dev |
| Computed fields (ETo, water deficit, GDD, chill hours, risk indices) | ✅ Done | Daily cron writes to DB. Blocks page now queries `soil_water_latest`, `phenology_latest`, `weather_latest` views and displays real values. When a sensor reading arrives within 24 h the cron carries its `soil_moisture` into the computed row. |
| DB schema / migrations | ✅ Done | 13 migrations applied — see table list below. Latest: `20260618000000_add_calendar_event_materials.sql` |

---

## 🤖 AI Reasoning Engine

| Requirement | Status | Notes |
|---|---|---|
| Netlify AI Gateway integration | ✅ Done | `NETLIFY_AI_GATEWAY_URL` env var swaps base URL; falls back to OpenRouter in dev |
| Prioritised agronomic recommendations | ✅ Done | Claude Haiku generates per-block recommendations from live DB context |
| Confidence scoring | ✅ Done | AI returns 0–100 score, shown on card |
| Accept / Edit / Skip feedback loop | ✅ Done | Accept + Edit write to activity_log + call applyStateMutation; Skip recorded on recommendation |
| Feedback writes back to block state | ✅ Done | `applyStateMutation`: irrigate → soil_water_readings reset to field_capacity + alerts cleared; spray → pest_observations resolved; all → domain alerts resolved |

---

## 🧭 Navigation

| Requirement | Status | Notes |
|---|---|---|
| Top nav bar | ✅ Done | `TopNav.tsx` exists |
| Sidebar | ✅ Done | `Sidebar.tsx` with links to all pages |
| Fully responsive (desktop + mobile) | ✅ Done | Mobile phases 1–4 complete: bottom nav, 44px targets, per-page 375px audit, iOS safe-area fixes |

---

## 🌍 Multi-Farm Support

| Requirement | Status | Notes |
|---|---|---|
| Farm selector screen after login | ✅ Done | `/farms` page with card grid; auto-opens wizard if 0 farms |
| Create farm wizard (name + GPS) | ✅ Done | 3-step modal: name/address → GPS coordinates → success |
| Farm switcher in sidebar | ✅ Done | FarmSwitcher compact widget + "All Farms" link in Sidebar |
| Farm ID in URL (bookmarkable) | ✅ Done | All routes under `/{farmId}/...` (e.g. `/{farmId}/dashboard`) |
| Per-farm isolated data (blocks, dashboard, calendar, activity, inventory) | ✅ Done | All server queries filtered by `farm_id` via `farm_members` membership |
| Per-farm, per-user role scoping | ✅ Done | `farm_members` table with per-farm role; layout enforces membership |
| **DB migration** | ✅ Done | `farms`, `farm_members` tables + `blocks.farm_id` column created with RLS policies applied 2026-05-30 |

---

## 🌐 Localisation

| Requirement | Status | Notes |
|---|---|---|
| English | ✅ Done | Default language |
| Arabic | ✅ Done | Full translations for all UI text; RTL layout throughout; locale-aware dates/day names via Intl |
| Turkish | ✅ Done | Full translations for all UI text; locale-aware dates/day names via Intl |
| Language preference persisted per user | ✅ Done | Cookie-based (`NUTJOB_LOCALE`) via `setLocale` server action in Settings → Language tab |
| RTL layout mirroring (nav, sidebar, bottom nav, modals, forms) | ✅ Done | `dir="rtl"` on `<html>`, logical Tailwind props (`me-`, `ps-`, `pe-`, `border-e`, `start-*`, `end-*`), `rtl:rotate-180` on directional icons |
| Arabic font | ✅ Done | Noto Sans Arabic loaded conditionally when `locale === 'ar'`; Geist with `latin-ext` for Turkish |
| Language switcher UI | ✅ Done | Globe icon + one-click dropdown in **TopNav** (visible on every page); simplified mirror in Settings → Language tab |
| Locale-aware date/weekday formatting | ✅ Done | `Intl.DateTimeFormat(locale, ...)` in WeatherStrip and UpcomingCalendar |
| Locale-aware number, date, and calendar formats | ✅ Done | `Intl.NumberFormat(locale)` via `utils/format.ts` — KPIGrid, WeatherStrip, BlockStatusGrid use `formatPercent`, `formatTemp`, `formatMeasurement`, `formatNumber` for locale-aware rendering (Eastern Arabic numerals for ar, comma decimals for tr) |
| AI recommendations returned in user's active language | ✅ Done | `generateAIRecommendations` reads `getLocale()`, appends language instruction to system prompt when locale ≠ en |

---

## 📱 Mobile Optimisation

| Requirement | Status | Notes |
|---|---|---|
| Touch-friendly tap targets (min 44×44 px) | ✅ Done | Nav items, bell, search icon all ≥ 44 px |
| Bottom navigation bar on small screens | ✅ Done | `BottomNav.tsx` — 4 primary items + More drawer; role-gated; `md:hidden` |
| Swipe gestures (blocks, calendar views) | ✅ Done | Calendar: left/right swipe navigates periods; Blocks: left/right swipe cycles the selected block in `BlocksPage.tsx` (same 50px-threshold touchstart/touchend pattern) |
| Service worker / offline-capable reads | ✅ Done | `public/sw.js` — cache-first for static assets, network-first for /dashboard /blocks /calendar; `manifest.json` for PWA install |
| Background sync for offline activity log entries | ✅ Done | `localStorage` queue + `online` event retry; pending entries shown with amber badge; synced flash on reconnect |
| Lazy-loading of images and chart data | ✅ Done | Modal dynamic imports (9 modals); BlockDetailPanel dynamic import; Dashboard Suspense streaming per section |

---

## 🔐 Roles & Permissions

| Requirement | Status | Notes |
|---|---|---|
| Admin role | ✅ Done | Full access to Settings > Team, deletes blocks, invites members |
| Worker role (log activities only) | ✅ Done | Read-only map, hides recommendations/settings, completion-only calendar |
| Supervisor role (change/add activities) | ✅ Done | Map boundary editing, log test, add calendar event, worker creation |
| Per-module permission enforcement | ✅ Done | Enforced securely on server-side layouts, server actions, and UI elements |

---

## 🛡️ Super Admin (platform-level, not in the original 8-page spec)

| Requirement | Status | Notes |
|---|---|---|
| `super_admin` global role | ✅ Done | `20260817000000_add_super_admin_role.sql` adds the enum value; granted manually via SQL (`UPDATE user_profiles SET role = 'super_admin' ...`), never self-service |
| `/admin` route guard | ✅ Done | `app/(superadmin)/admin/layout.tsx` — layout-level check (not middleware), mirrors `[farmId]/(dashboard)/layout.tsx`'s precedent; redirects non-super-admins to `/farms`, logged-out to `/login` |
| Organization/billing data model | ✅ Done | `20260817100000_create_organizations.sql` — `organizations` + `organization_members` tables per the B2B design in `Requirements.md`; `farms.organization_id` FK (nullable, no backfill of legacy farms); `farm_seats` defaults to 3 to match the old hardcoded farm cap it replaces (`app/actions/farms.ts`'s `createFarm` now gates on org seat count instead) |
| Stripe checkout + webhook sync | ✅ Done | `utils/stripe.ts` client; `createCheckoutSession` (`app/actions/organizations.ts`) creates/reuses a Stripe customer and a subscription-mode Checkout session; `app/api/webhooks/stripe/route.ts` verifies signature and syncs `subscription_status`/`farm_seats` on checkout/update/cancel/payment-failure, caching paid invoices into `stripe_invoices` (`20260817200000_create_stripe_invoices_cache.sql`, no client-readable RLS policy at all) |
| Cross-farm overview | ✅ Done | `/admin/overview` — every farm, its owner, its organization's plan, member/block counts |
| Subscribers list + detail | ✅ Done | `/admin/subscribers` list (plan, seats used, member/farm counts) and `/admin/subscribers/[orgId]` detail (members, farms, Stripe payment history with links to hosted invoices) |
| Org-owner self-service Billing UI | 🟡 Deferred | `createCheckoutSession` exists as shared infra, but no Settings → Billing tab calls it yet — org owners currently have no UI path to actually subscribe |
| Past-due grace-period enforcement | 🟡 Deferred | Webhook sets `subscription_status = 'past_due'`, but no farm-scoped action reads it yet to gate writes |

---

## 📊 Summary

| Area | Progress |
|---|---|
| Project scaffold & tech stack | ✅ 100% |
| Auth (basic login) | ✅ 100% |
| Dashboard page | ✅ 100% (Wired to live Supabase data & Open-Meteo) |
| Settings page | ✅ 100% (All 7 tabs: Account, Team, Block Config, Alert Thresholds, Sensors, Weather & AI, Language) |
| Navigation | ✅ 100% (TopNav + Sidebar + BottomNav with More drawer, all role-gated) |
| Blocks page | ✅ 100% (Phase 4 — satellite map with GPS polygon draw/edit, Go-to-location bar) |
| Recommendations page | ✅ 100% (UI + edit modal + activity log + AI generation + priority ordering done) |
| Activity Log page | ✅ 100% (+ manual Log Activity button for supervisor/admin) |
| Inventory page | ✅ 100% (Asset & Consumable tracking with calendar linking) |
| Database schema | ✅ 100% |
| Real data ingestion | ✅ Done (weather cron ✅; computed fields cron ✅; sensor ingest pipeline ✅; manual logs ✅) |
| AI reasoning engine | ✅ 100% (generation, confidence scoring, accept/edit/skip, state mutation, block state feedback loop all done) |
| Roles & permissions | ✅ 100% |
| Multi-farm support | ✅ 100% |
| Localisation (Arabic, Turkish) | ✅ 100% (next-intl + 3 message files + RTL + language switcher + locale dates + locale-aware numbers + AI in user language) |
| Mobile optimisation | ✅ 100% (bottom nav, tap targets, swipe on calendar + blocks, service worker, per-page layout, offline sync, lazy-load all done) |
| Super Admin (platform-level) | 🟡 ~80% (role gate, org/billing data model, Stripe checkout + webhook sync, overview/subscribers/detail UI all done; org-owner self-service Billing UI and past-due enforcement still deferred) |

---

## 📝 Changelog

| Date | What changed |
|---|---|
| 2026-05-09 | Initial progress snapshot created |
| 2026-05-09 | Built Calendar page (Month/Week/Day views, Add Event & Log Completion modals, mock data) |
| 2026-05-09 | Created `deploy-skill.md` with instructions for deploying the NutJob application to the local machine |
| 2026-05-17 | Built Blocks page Phase 2 — wired up Supabase persistence, created Server Actions for block creation, fixed RLS, and added optimistic UI updates |
| 2026-05-17 | Built complete Supabase schema — 13 tables: user_profiles, blocks (seeded), block_alerts, soil_water_readings, phenology_records, tissue_samples, fertigation_log, scouting_reports, pest_observations, weather_snapshots, activity_log, recommendations + upgraded calendar_events. All tables have RLS + policies. |
| 2026-05-19 | Built Recommendations page scaffolding (route, server fetch, card grid, accept/skip), then added edit modal — manager can tweak the AI title, add a personal note, and accept with changes (status → "edited"); manager note shown in history view. |
| 2026-05-19 | Wired Accept → activity_log write-back. Accept (and "Accept with changes") now inserts an activity_log row (mapped activity_type, block_id, rationale, performed_by, JSON details) and stores the returned activity_log_id on the recommendation, closing the feedback loop. |
| 2026-05-19 | Implemented AI generation — installed @anthropic-ai/sdk, added generateAIRecommendations server action that gathers per-block context (soil, weather, alerts, scouting, tissue samples), calls Claude Haiku with a cached system prompt, validates JSON output, and inserts into Supabase. Added "Generate AI Insights" button to the toolbar. |
| 2026-05-19 | Built Activity Log page — server route + ActivityLogClient with live search by title, filter by activity type and block, entry count, colour-coded type badges, and empty state. Data sourced from Supabase activity_log table (written to by accepted recommendations). |
| 2026-05-19 | Wired calendar completions → activity_log. Fixed three bugs: (1) createEvent now returns the real Supabase UUID so the optimistic local state is patched and completions use the correct ID; (2) activity_log insert uses createAdminClient to bypass RLS; (3) performed_by set to user.id (UUID) not user.email. |
| 2026-05-19 | Enhanced farm map: north compass SVG overlay (top-right of map, brand-colour N arrow), dynamic grid sizing (expands beyond 3 columns automatically), map position inputs (column, row, col span, row span) in BlockFormModal, and updateBlock now persists map position to Supabase. |
| 2026-05-19 | Lab test result logging: LogTestResultModal with block selector, date, lab ref, pH/EC/moisture/temp/deficit inputs, notes, and PDF/image file attachment. logTestResult server action uploads file to lab-reports Supabase storage bucket and inserts manual reading into soil_water_readings. SoilWaterTab shows Lab Test History section with signed-URL file link. |
| 2026-05-19 | Expanded lab test modal to full Turkish lab report standard: Soil Test (pH, EC, organic matter, P₂O₅, K₂O, lime, CEC, Ca, Mg, Na, Fe, Zn, Cu, Mn, B, soil texture) and Water Test (pH, EC in µs/cm). Added live benchmark status badges per parameter. Added "Whole Farm" scope option (null block_id). Lab Test History now shows colour-coded parameter chips with benchmark indicators. Added test_type and parameters JSONB columns to soil_water_readings. |
| 2026-05-21 | Phase 4 — Replaced CSS grid farm map with Leaflet satellite map (Esri World Imagery, no API key). User draws GPS polygon boundaries for each block in edit mode; polygons persist to Supabase `boundary` JSONB column. Added GoToLocationBar for jumping to non-adjacent farm plots by entering GPS coordinates. Map is static (view-only) by default; "Edit Farm Map" enables drawing tools; "Accept Changes" persists all boundary edits; "Cancel" reverts. BlockFormModal updated: removed grid position inputs, added boundary status badge. **Requires manual SQL migration**: `ALTER TABLE blocks ADD COLUMN IF NOT EXISTS boundary JSONB;` |
| 2026-05-24 | Phase 5 — Fully implemented user roles (admin, supervisor, worker), permissions gating, team/user management dashboard inside settings (changing roles, creating users/workers), and session persistence ("Remember Me" logic) at login. ESLint clean with 0 warnings/errors. |
| 2026-05-24 | Phase 6 — Fully integrated dashboard layer with Supabase live database. Connected KPIGrid (dynamic moisture, rain totals, alert counters), ActiveAlerts (live unresolved list), BlockStatusGrid (real block health), UpcomingCalendar (live events), and ActivityFeed (completed actions log). Completed full linter and production build compilation verify with 0 warnings/errors. |
| 2026-05-24 | AI soil/water test result extraction: Replaced Turkish-only regex/positional PDF parser with an advanced multimodal Gemini 2.5 Flash processor via OpenRouter and Trigger.dev. Accepts both PDF and image (PNG, JPEG, etc.) uploads. extracts pH, EC, macronutrients, minerals, and soil texture directly into the LogTestResultModal. |
| 2026-05-24 | Robust Soil Extraction Fix — Enhanced `extract-soil-test` API route to support direct, self-contained OpenRouter LLM extraction when running locally, with a graceful Trigger.dev fallback. Resolves local Trigger.dev authentication and `ApiClientMissingError` crashes on server start. |
| 2026-05-24 | Phase 7 — Built Inventory Page: Asset and Consumable management. Added local-first data store for asset status, maintenance logs, and consumable balances, complete with low-stock alerts, searchable dropdown suggestions, calendar event usage linking, and full role-based permissions (admin/supervisor vs worker read-only). |
| 2026-05-26 | Option A — (1) Fixed recommendations ordering: pending cards now sorted by confidence DESC (highest urgency first), with created_at DESC as tiebreaker. (2) Added "Log Activity" manual quick-entry to Activity Log page: `LogActivityModal` (activity type grid, title, block, datetime, notes) with `logActivity` server action (admin client bypass of RLS), optimistic prepend to list, and role-gating (supervisor/admin only). TypeScript clean — 0 errors. |
| 2026-05-26 | Settings Page completed — Rebuilt `SettingsForms.tsx` with 6 role-gated tabs: (1) Account & Security, (2) Team Management, (3) Block Configuration (per-block field_capacity/wilting_point/notes with Supabase persistence + live progress bars), (4) Alert Thresholds (5 sliders in localStorage: soil moisture, water deficit, heat stress, rain skip, pest risk), (5) Sensor Connections (channel status + copy-to-clipboard ingest endpoint reference), (6) Weather & AI (farm lat/lng with Open-Meteo live test + Netlify AI Gateway config). TypeScript clean — 0 errors. |
| 2026-05-26 | AI engine switched to direct OpenRouter calls — Removed Trigger.dev dependency from `generateAIRecommendations`; logic now runs inline in the server action using the OpenAI SDK pointed at `https://openrouter.ai/api/v1` with model `google/gemini-2.5-flash`. Context gathering (blocks, soil, weather, alerts, scouting, tissue), prompt, JSON parsing, validation, and DB insert all inlined. Requires `OPENROUTER_API_KEY` env var. |
| 2026-05-26 | Lint clean sweep — Fixed all 20 ESLint errors: removed unused `gemini`/`AI_SYSTEM_PROMPT` from recommendations actions, converted two `useEffect` setState calls to lazy state initializers in SettingsForms, escaped apostrophe entity, added file-level any-disable in inventory actions, removed unused imports (`ASSET_SUGGESTIONS`, `Filter`, `isPending`), typed `recentCalendarEvents` and `events` props properly. 0 errors, 0 warnings. |
| 2026-05-26 | Requirements updated — added three new requirements: (1) Multi-farm support (single login, farm selector, per-farm isolated data and role scoping); (2) Localisation to Arabic and Turkish with RTL layout, locale-aware formatting, and AI responses in active language; (3) Mobile-first optimisation (bottom nav, swipe gestures, service worker offline reads, background sync, lazy loading <3 s on 4G). |
| 2026-05-26 | Branding — Replaced placeholder "N" icon in Sidebar and Leaf icon in Login page with the NutJob tree+almond logo mark (cropped from brand asset, saved as `public/icon.png`). |
| 2026-05-26 | Branding refinement — Login splash screen updated to use full logo (`logo-full.png`: tree+almond mark + "NutJob almond farms" + "HARVESTED WITH CARE") with transparent background. Added radial back-light glow behind the logo on the dark green panel. Sidebar retains icon-only mark with transparent background. |
| 2026-05-29 | Lazy-loading — 9 modal components converted to `next/dynamic` (ssr: false) across InventoryPage, CalendarPage, BlocksPage, ActivityLogClient. BlockDetailPanel dynamically imported with pulse skeleton in BlocksPage. Dashboard refactored: each section (KPIGrid, ActiveAlerts, BlockStatusGrid, UpcomingCalendar, ActivityFeed) now self-fetches its own data as an async server component, wrapped in individual `<Suspense>` boundaries with pulse skeletons so the page shell renders instantly. Build clean — 0 errors. |
| 2026-05-28 | Mobile optimisation phase 4 — Per-page layout audit and fixes for 375px viewport: LogTestResultModal all multi-column grids → responsive (`grid-cols-1 sm:grid-cols-3`, `grid-cols-2 sm:grid-cols-4`); SoilWaterTab param-chips grid `grid-cols-4` → `grid-cols-2 sm:grid-cols-4`; BlockDetailPanel 5-tab bar reduced to `px-2 sm:px-3 text-xs sm:text-sm`; Settings 6-tab bar reduced to `px-3 sm:px-4 text-xs sm:text-sm`; all inventory modals (AddAsset, AddConsumable, LogMaintenance, LogUsage) and LogActivityModal block/date grid → `grid-cols-1 sm:grid-cols-2`. Build clean — 0 errors. |
| 2026-05-28 | Mobile optimisation phase 3 — Service worker (`public/sw.js`): cache-first for `/_next/static/` and public images; network-first with offline HTML fallback for /dashboard, /blocks, /calendar; skips RSC navigation payloads to avoid breaking Next.js routing. Added `public/manifest.json` (PWA install, standalone display, brand green theme). `ServiceWorkerRegistration.tsx` client component registers SW on mount. Root layout exports `appleWebApp` metadata for iOS add-to-home-screen. Build clean. |
| 2026-05-28 | Mobile optimisation phase 2 — Calendar swipe gestures (left = next period, right = prev, 50 px threshold); CalendarHeader prev/next buttons enlarged to 44×44 px on mobile; TopNav converted to client component with ← page-title back button on sub-pages (logo shown on dashboard only). |
| 2026-05-28 | Mobile optimisation phase 1 — Added `BottomNav.tsx`: fixed bottom bar with Dashboard, Blocks, Calendar, Activity primary items + "More" slide-up drawer (Recommendations, Inventory, Settings, Sign Out), role-gated for workers. Sidebar hidden on mobile (`hidden md:flex`). TopNav updated: brand logo on mobile, collapsible search icon, bell tap target enlarged to 44×44 px. Dashboard layout adds `pb-24` on mobile for bottom nav clearance. Root layout exports `viewport` with `viewportFit: "cover"` for iOS safe areas. Build clean — 0 TS errors. |
| 2026-05-29 | Multi-farm support — Full architecture implemented. New `farms` + `farm_members` tables with RLS. All routes restructured to `/{farmId}/dashboard`, `/{farmId}/blocks`, etc. Farm picker page (`/farms`) with card grid and "New Farm" 3-step wizard (name → GPS → success). Farm switcher widget in sidebar. All dashboard widgets, blocks page, calendar, activity, and inventory queries scoped to the current farm via `farm_members` membership. Login redirects to `/farms` after auth. **Requires DB migration** — SQL in plan file. |
| 2026-05-29 | Farm tab bar UX fix — FarmTabs now always renders exactly 3 slots (filled or empty). Empty slots use a dashed "+" placeholder styled per-slot colour; clicking any empty slot opens the CreateFarmWizard. Fixes the 4-tab bug (was rendering filled farms + a separate empty slot, totalling more than 3 when at capacity). |
| 2026-05-29 | Farm rename in Settings — Added "Farm Identity" section at the top of the Weather & AI tab (admin only). Shows current farm name and address with Save button wired to the existing `updateFarm` server action. Farm name in the right-edge tab bar updates on next navigation. |
| 2026-05-29 | Farm creation flow simplified — CreateFarmWizard reduced to 2 steps (name/location → GPS). On completion it navigates directly to the new farm's dashboard, which already shows the "Welcome" onboarding cards (Add block, Log soil tests, etc.). Removed redundant success step that duplicated the same setup cards. Also removed FarmSwitcher from Sidebar (caused hydration error and showed a confusing "All Farms" link); farm switching is now handled exclusively by the right-edge tab bar. |
| 2026-05-30 | DB migration applied — `farms` and `farm_members` tables created with full RLS policies; `farm_id` FK column added to `blocks`. Multi-farm support now fully operational end-to-end. |
| 2026-05-30 | Locale-aware number formatting — created `utils/format.ts` with `formatPercent`, `formatTemp`, `formatMeasurement`, `formatNumber` helpers using `Intl.NumberFormat`. KPIGrid, WeatherStrip, BlockStatusGrid updated to render locale-aware values (Eastern Arabic numerals in `ar`, comma decimals in `tr`). |
| 2026-05-30 | Netlify AI Gateway wired — `NETLIFY_AI_GATEWAY_URL` env var now swaps the base URL in `generateAIRecommendations`; falls back to OpenRouter in dev. AI recommendations also return text in user's active language (`getLocale()` + language instruction injected into system prompt). |
| 2026-05-30 | Scheduled weather cron — `/api/cron/weather` route fetches current conditions + 7-day forecast from Open-Meteo for every farm with GPS coordinates. Stores farm-level and per-block `weather_snapshots` rows. Secured with `CRON_SECRET`. Designed for Netlify Scheduled Functions (every 3 hr) or any external cron trigger. |
| 2026-05-30 | Netlify deployment config — `netlify.toml` (build command, `.next` publish dir, `@netlify/plugin-nextjs`, functions directory). `netlify/functions/cron-weather.mts` — Netlify Scheduled Function that calls `/api/cron/weather` every 3 hours via `CRON_SECRET` + `URL` env vars. |
| 2026-05-30 | AI feedback loop confirmed done — `applyStateMutation` in recommendations/actions.ts already handles: irrigate → insert soil_water_readings reset to field_capacity + resolve soil-water alerts; spray → resolve pest_observations + alerts; all categories → resolve domain alerts. PROGRESS.md updated to reflect ✅ status. |
| 2026-05-31 | Pre-publish security review — 6 fixes applied: (1) Removed hardcoded local developer path (`C:/Users/mhrg7/...`) and `fs`/`path` debug block from `extract-soil-test` route; (2) Added Supabase auth check to all unauthenticated API routes (`extract-soil-test`, `plant-search`, `plant-varieties`) to prevent LLM/API cost abuse; (3) Added 20 MB file size limit to `extract-soil-test` upload; (4) Added HTTP security headers to `next.config.ts` (X-Frame-Options, X-Content-Type-Options, Referrer-Policy, Permissions-Policy, Content-Security-Policy); (5) Wrapped `JSON.parse(boundary)` in try-catch in `blocks.ts` (create + update actions); (6) Escaped SQL wildcards (`%`, `_`, `\`) in `ilike` search in both activity actions files. |
| 2026-06-01 | Fix cross-farm weather forecast bug (round 1) — (1) `next.config.ts`: added `experimental.staleTimes.dynamic = 0` so the Next.js client-side Router Cache never reuses a stale layout RSC payload carrying the previous farm's `farmId`; (2) layout: added `export const dynamic = 'force-dynamic'`; (3) `/farms` page: show `FarmPicker` for multi-farm users instead of always redirecting to Farm 1; (4) `CreateFarmWizard.tsx`: added `router.refresh()` after `router.push`; (5) `blocks.ts`: `createBlock`, `updateBlock`, `updateBlockBoundary` now also revalidate the dashboard path. |
| 2026-06-01 | Fix Settings GPS not saving to database (root cause) — The Settings → Weather & AI tab GPS lat/lng inputs were saving to `localStorage` only (never to Supabase), while `getFarmCoords` reads exclusively from the database. Fixed: (1) `SettingsForms.tsx`: `WeatherAPITab` now accepts `initialLat`/`initialLng` props (DB values), initialises state from them instead of localStorage, and `handleSave()` calls `updateFarm(farmId, { gps_lat, gps_lng })` to persist to Supabase with success/error feedback; (2) `settings/page.tsx`: fetches `gps_lat, gps_lng` from the farms table and passes them as props; (3) `updateFarm` now also revalidates the dashboard path; removed `STORAGE_KEY_WEATHER` and hardcoded Antalya default coordinates. |
| 2026-06-02 | AI reasoning enriched with tree age and phenological growth stage — `src/trigger/recommendations.ts` now fetches `phenology_latest` view alongside existing queries, computes tree age from `planting_year`, and appends both to each block's context string sent to Gemini Flash (growth stage, GDD, chill hours, days to hull split, estimated harvest window). System prompt updated with a rule to use age and stage when tailoring recommendation intensity and category-gating. Degrades gracefully when no phenology record exists for a block. |
| 2026-06-02 | PROGRESS.md cleanup — Added Inventory Page as numbered section 7 (was missing despite being done); renamed Settings to section 8; updated page count from 7 to 8; corrected "Last updated" header date. |
| 2026-06-02 | Computed fields daily cron — New `/api/cron/compute-fields` route fetches daily Tmax/Tmin from Open-Meteo and writes per-block computed rows to `phenology_records` (cumulative GDD, chill hours, inferred growth stage) and `soil_water_readings` (ETo via Hargreaves-Samani, 7-day forward water deficit). Idempotent: skips blocks already computed today. `netlify/functions/cron-compute-fields.mts` schedules it at midnight daily. All computation uses existing `utils/agronomic.ts` helpers. |
| 2026-06-02 | IoT sensor infrastructure — Full sensor readiness: (1) `supabase/migrations/20260602000000_create_sensors.sql` — `sensors` table with farm/block FK, api_key (unique), status, last_seen_at; `sensor_id` FK added to `soil_water_readings` and `weather_snapshots`; (2) `types/sensors.ts` — application-level sensor types; (3) 5 new server actions in `settings/actions.ts` — `registerSensor`, `updateSensor`, `deleteSensor`, `assignSensorToBlock`, `generateSensorApiKey`; (4) Settings > Sensor Connections tab rebuilt — live sensor inventory table with add/edit/delete, API key copy+regenerate, one-time key display banner; (5) 3 authenticated ingest API routes — `POST /api/ingest/soil`, `/weather`, `/alert` with `X-Sensor-Key` header auth; (6) Blocks page (`[farmId]/blocks/page.tsx`) now runs 4 parallel Supabase queries and builds real `initialProfiles` from `soil_water_latest`, `phenology_latest`, `weather_latest`, `block_alerts`, and `sensors` — no more all-zeros mock data; (7) `SoilWaterTab` shows "N sensors monitoring · updated Xm ago" line when sensors are assigned to a block; (8) Cron enhanced to carry latest sensor `soil_moisture` into the daily computed row when a reading arrived within 24 h. |
| 2026-06-02 | Manual logs — `LogActivityModal` extended with activity-specific structured fields: Irrigation (duration h, volume L/tree, method dropdown); Fertigation (product name, amount+unit, growth stage note); Spraying (product, rate L/ha, target pest, PHI days); Scouting (Low/Medium/High risk buttons, observations, next scouting date); Pruning (type dropdown, intensity buttons). Structured `ActivityDetails` discriminated union exported from `actions.ts` and stored in `activity_log.details` JSONB. Fertigation also side-writes to `fertigation_log` (when block is set + amount provided); Scouting also side-writes to `scouting_reports` (when block is set). Both side-writes are guarded and swallowed on failure. `getActivityLog()` now selects `details` column. `ActivityLogClient` renders structured detail chips inline per entry. Offline queue carries details through sync. Fixed stale import path bug in `LogActivityModal` (was pointing to old non-farm-scoped actions file). Build clean — 0 errors. |
| 2026-06-03 | Fix mobile bottom nav hidden on iOS — `BottomNav` nav bar now adds `paddingBottom: env(safe-area-inset-bottom)` so it renders above the iPhone home indicator and Safari floating toolbar (previously hidden at `bottom: 0` under the home indicator). Drawer `bottom` updated to `calc(64px + env(safe-area-inset-bottom))`. Dashboard layout `<main>` bottom padding updated to `calc(6rem + env(safe-area-inset-bottom))` so page content is not obscured. |
| 2026-06-03 | Mobile farm switcher — `BottomNav` "More" drawer now shows a "My Farms" section listing all user farms when the user has more than one. Tapping a farm preserves the current page slug and navigates to that farm. Translated into EN/AR/TR. Previously, farm switching was only available on desktop via the right-edge tab bar (`FarmTabs`). |
| 2026-06-03 | Fix "More" button not tappable — closed drawer (`translate-y-full`) was silently intercepting all nav bar taps because it slides to exactly cover the nav (z-50 > z-40). Added `pointer-events-none` when drawer is closed. |
| 2026-06-03 | Fix mobile nav hidden behind Safari browser toolbar (root fix) — `position: fixed; bottom: 0` resolves against the layout viewport (behind the toolbar), not the visual viewport. Replaced with a flex-child nav: body now uses `h-dvh` (dynamic viewport height = visible area only); `BottomNav` moved inside the flex layout as a regular block element; drawer changed from `fixed` to `absolute bottom-full`; closed-state animation changed from `translate-y-full` to `opacity-0 invisible translate-y-2`. |
| 2026-06-03 | Fix soil test save failure — `logTestResult` server action was using `createClient()` (anon-key session client) for the Supabase INSERT, which is blocked by RLS on `soil_water_readings` (no INSERT policy for authenticated users). Switched to `createAdminClient()` (service role key, bypasses RLS), consistent with the file upload in the same action and all other server-side writes in the project. |
| 2026-06-16 | Fix soil test save on Netlify — `createAdminClient()` throws if `SUPABASE_SERVICE_ROLE_KEY` is missing; `handleSave` had no try/catch so the error was silently swallowed (spinner stuck, no message). Added try/catch that surfaces the error in the modal's error banner. Also fixed history not refreshing: replaced browser-client SELECT (blocked by RLS) in `SoilWaterTab` with a `getLabReadings` server action using `createAdminClient()`; added `soilRefreshKey` counter in `BlocksPage` that increments on modal close to trigger a re-fetch. |
| 2026-06-16 | Fix soil test history on Netlify — root cause was the Supabase `sb_secret_` key format not bypassing PostgREST RLS as the old JWT `eyJ...` service role key did. INSERT was already working via `createClient()` with the user session + a permissive INSERT RLS policy. SELECT was blocked because: (1) browser client SELECT ignores errors silently; (2) admin client SELECT with `sb_secret_` key doesn't bypass RLS; (3) the farm-scoped SELECT policy referenced `blocks`/`farm_members` but those tables weren't resolving. Fixed by: switching `SoilWaterTab` to call a `getLabReadings` server action (uses `createClient()` with cookie session, same as the working INSERT), simplifying the SELECT RLS policy to `USING (true)` for all authenticated users, and removing all dead code/debug instrumentation. |
| 2026-06-05 | Web Push notifications — Full VAPID push pipeline: (1) `supabase/migrations/20260606000000_create_push_subscriptions.sql` — `push_subscriptions` table with RLS, unique endpoint constraint, indexed by farm/user; (2) `utils/push.ts` — `sendPushToFarm()` server utility (web-push + admin client, auto-purges stale 410/404 subs); (3) `POST /api/push/subscribe` and `DELETE /api/push/unsubscribe` API routes with farm membership verification; (4) `public/sw.js` — added `push`, `notificationclick`, `notificationclose` service worker handlers; (5) `ServiceWorkerRegistration.tsx` — exported `subscribeToPush(farmId)` and `unsubscribeFromPush()` browser helpers; (6) Settings → Alert Thresholds tab — added `PushNotificationToggle` at the top (toggle switch with permission state detection, denied-state guidance, success feedback); (7) `/api/ingest/alert` — fires push on warning/critical severity (non-blocking); (8) `generateAIRecommendations` — fires push per farm after successful insert; (9) TopNav bell badge — replaced hardcoded "3" with live unresolved `block_alerts` count from DB; bell icon is now a link to dashboard. Requires VAPID env vars: `VAPID_PUBLIC_KEY`, `VAPID_PRIVATE_KEY`, `VAPID_SUBJECT`, `NEXT_PUBLIC_VAPID_PUBLIC_KEY`. |
| 2026-06-17 | AI Agronomist data freshness matrix + weekly schedule — Introduced a formal three-tier data model: SLOW (6-monthly: soil lab tests, block static metadata, regional climate normals from farm GPS), WEEKLY (7-day forecast, tissue samples, scouting, phenology), DAILY (IoT sensors 3×/day at 8-hour intervals). Key changes: (1) `supabase/migrations/20260617000000_add_climate_cache_to_farms.sql` — adds `climate_profile JSONB` + `climate_fetched_at TIMESTAMPTZ` to `farms`; (2) `utils/climate-profile.ts` — fetches 3-year monthly normals from Open-Meteo archive API (daily Tmax/Tmin/precip), computes aridity class, peak irrigation months, frost risk months, caches in DB for 180 days; (3) `utils/build-block-context.ts` — single shared context builder used by both background task and server action; separates daily IoT query (last 25h only, sensor/computed source) from lab soil test query (latest ever, test_type=soil/water); includes timestamps and staleness warnings per tier; (4) `src/trigger/recommendations.ts` — converted from `task` to `schedules.task` with `cron: "0 4 * * 1"` (Monday 04:00 UTC automatic weekly run); sets `expires_at = now() + 7 days` on each inserted recommendation; (5) `app/(dashboard)/recommendations/actions.ts` — uses shared context builder and updated system prompt; also sets `expires_at`; (6) `RecommendationsClient.tsx` — shows "Generated: [date] · Valid until: [date] · Refreshed weekly" banner above pending cards; banner turns amber when expiry is within 48 hours. |
| 2026-06-17 | SenseCAP IoT sensor integration — Full end-to-end pipeline to pull live LoRaWAN/4G sensor telemetry from the SenseCAP cloud into the farm block state 3× per day (00:00, 08:00, 16:00 UTC). (1) `supabase/migrations/20260617100000_add_sensecap_to_farms.sql` — adds `sensecap_api_id` + `sensecap_access_key` columns to `farms`; (2) `utils/sensecap-client.ts` — typed HTTP client (HTTP Basic Auth), `MEASUREMENT_MAP` converting SenseCAP measurement IDs (4097–4113) to our schema fields with wind speed m/s→km/h transform, `mapTelemetryToPayloads` to split channels into soil/weather payloads; (3) `app/api/cron/sensecap-sync/route.ts` — pull cron: fetches credentials from DB, loops over sensors with `device_id` set (SenseCAP EUI), deduplicates by `(sensor_id, recorded_at)`, inserts to `soil_water_readings` and `weather_snapshots` with `source='sensor'`, updates `sensors.status` + `last_seen_at`; (4) `src/trigger/sensecap-sync.ts` — Trigger.dev `schedules.task` at `0 0,8,16 * * *` invoking the cron endpoint; (5) Settings → Sensor Connections tab: new "SenseCAP Cloud Integration" sub-section with API ID input, masked Access Key field with show/hide toggle, Save Credentials and Test Connection buttons wired to new `saveSensecapCredentials` and `testSensecapConnection` server actions; (6) `utils/supabase/farm-types.ts` — added `sensecap_api_id` and `sensecap_access_key` to `Farm` interface; (7) `.env.local.example` — documented `NEXT_PUBLIC_APP_URL` required by Trigger.dev task. |
| 2026-06-18 | Calendar ↔ Inventory link — Full end-to-end integration: (1) `supabase/migrations/20260618000000_add_calendar_event_materials.sql` — `calendar_event_materials` junction table (FK to `calendar_events` + `consumables`, RLS); (2) Supabase types updated; (3) `AddEventModal` — Fertigation/Spraying event type sections now include an inline "Total Qty" field next to the consumable dropdown (no separate Planned Materials section for those types); selecting a consumable + filling in the qty is all that's needed; (4) `createEvent` server action saves `calendar_event_materials` rows; (5) Server page joins materials onto events at page load; (6) `CalendarPage.handleSaveEvent` enriches local event state with `MaterialLine[]` so the modal works without a page refresh; (7) `LogCompletionModal` initialises editable actual-qty inputs from planned materials; (8) `logEventCompletion` re-fetches live balance, inserts `consumable_usage_log`, decrements `consumables.current_balance`, revalidates inventory cache; (9) `AddStockModal` + `addStock` action — "+  Add Stock" button on each consumable row increments both `current_balance` and `starting_balance`; (10) Fixed TypeScript build errors in `utils/build-block-context.ts` and `utils/sensecap-client.ts` that were blocking Netlify deploys. |
| 2026-06-18 | Self-onboarding & OAuth — Added user self-registration via Email/Password signup and Google OAuth (Gmail) authentication, created OAuth callback route, updated LoginPage UI (Sign In/Up toggling, Google Auth button), updated messages for localization, and patched Supabase middleware to bypass login redirection for /api/ routes. |
| 2026-06-26 | GPS precision fix — Changed `gps_lat`/`gps_lng` columns from `numeric(x,2)` to `float8` via migration `20260626120000_gps_float_precision.sql`. CreateFarmWizard and SettingsForms GPS inputs now auto-truncate to 4 decimal places on change (no browser error for extra decimals); `createFarm` and `updateFarm` server actions also round to 4 decimal places before saving. `step` updated to `0.0001`. |
| 2026-06-26 | Multilingual crop name search — `/api/plant-search` now detects non-Latin characters in the user's input and translates to English via OpenRouter (Claude Haiku) before querying the curated crop list and Trefle API. English queries bypass translation entirely. Allows users to type crop names in Arabic, Turkish, French, etc. and get correct results. |
| 2026-06-28 | Favicon — Replaced the default Next.js favicon with the RootLoot.ai icon mark (circuit tree + golden diamond, cropped from `public/icon.png`, no text). Generated `app/icon.png` (512×512, transparent background) used by Next.js App Router metadata API, and `app/favicon.ico` (16×16, 32×32, 48×48 embedded PNGs) for legacy browser support. |
| 2026-07-02 | Security audit — (1) **Dependencies: 29 vulns (6 high) → 14 moderate (all one accepted advisory).** `npm audit fix` for babel; upgraded `next` 16.2.4 → 16.2.10 (13 advisories incl. middleware/proxy auth bypass, cache poisoning, XSS, DoS — critical since middleware guards all dashboard routes); upgraded `@trigger.dev/sdk`+`build` 4.4.6 → 4.5.0; `package.json` overrides force patched `ws@^8.21.0` (uninit memory disclosure), `cookie@^0.7.2` (scoped to engine.io — Supabase's cookie@1.x untouched), `postcss@^8.5.13`. Remaining 14 moderates are a single `@opentelemetry/core` advisory pinned by `@trigger.dev/core` — runs only in Trigger.dev's managed worker environment, not the web app; accepted until Trigger.dev bumps it. (2) **Deleted `/api/soil-readings`** — unauthenticated route using the service-role client (leaked lab readings to anyone) with PostgREST filter injection via `blockId`; dead code with zero references, superseded by the `getLabReadings` server action (2026-06-16). (3) **Cron endpoints fail closed** — `/api/cron/weather`, `/api/cron/compute-fields`, `/api/cron/sensecap-sync` previously skipped auth entirely when `CRON_SECRET` was unset (fail-open); now return 503 in production if unset. (4) **Verified per-route auth**: ingest routes (X-Sensor-Key), extract-soil-test/plant-search/plant-varieties (Supabase session), push subscribe/unsubscribe (session + farm membership), middleware page protection. Clean-rebuild + lint verified 0 errors. |
| 2026-07-02 | Launch-readiness pass — (1) **Lint: 163 errors / 1177 warnings → 0 / 0.** Added ESLint global ignores for generated `.trigger/` artifacts, local working folders, and `public/sw.js`; file-level `no-explicit-any` disables for untyped-Supabase-cast server files (build-block-context, climate-profile, farms, ingest routes, trigger tasks) consistent with existing precedent; targeted `react-hooks/set-state-in-effect` / `react-hooks/purity` disables with justifications for intentional modal-reset/offline-queue/relative-time patterns; fixed `prefer-const` in sensecap-sync, typed `blockFarmMap` callback, `Record<string, unknown>` in LogTestResultModal, added missing `farmId` dep in ActivityLogClient `applyFilters`, removed unused imports/vars; added `^_` ignore patterns for `no-unused-vars`. (2) **Build verified** — `npm run build` passes with 0 TS errors after all fixes. (3) **Config sweep** — `.env.local.example` completed with all env vars referenced in code (VAPID×4, GEMINI_API_KEY, TREFLE_API_KEY, TRIGGER_SECRET_KEY) and un-gitignored via `!.env*.example` (was silently excluded by `.env*` — template never reached the repo); README.md replaced (was create-next-app boilerplate) with real project overview, feature list, setup, and deployment guide; `.claude/settings.local.json` untracked + gitignored (machine-local). (4) Navigation responsive row 🟡→✅ (mobile phases 1–4 previously completed and verified). |
| 2026-07-03 | Google OAuth redirect fix — `NEXT_PUBLIC_APP_URL` had a trailing slash, producing a double-slash `redirectTo`/`emailRedirectTo` (`.../auth/callback` → `..//auth/callback`) that Supabase's Redirect URL allow-list rejected, silently bouncing users back to `/login` with no code exchanged and no error shown. Fixed the local `.env.local` value and made `app/(auth)/login/actions.ts` strip trailing slashes from `NEXT_PUBLIC_APP_URL` defensively for both Google OAuth and email-confirmation redirects. **Action required**: verify the same env var has no trailing slash in the Netlify production environment and redeploy. |
| 2026-07-03 | Mobile topbar logo fix — The `f8702dd` "paper/ink" reskin swapped the mobile topbar logo (`TopNav.tsx`, `md:hidden` branch) from `/icon.png` to `/logo-dark-transparent.png`, a light/white-colored wordmark meant for dark panels; rendered on the topbar's near-white `bg-paper-2` it washed out to near-invisible. Reverted `src` back to `/icon.png` (dark-colored mark, correct for light backgrounds per the design handoff spec — same fix pattern already applied to the login page and sidebar logos via `mix-blend-screen`). Verified via direct image inspection and dev-server asset serving; full authenticated-session mobile screenshot not captured (no test credentials / chromium-cli available in this environment). |
| 2026-07-03 | Fixed stuck-farm onboarding bugs — (1) **Settings falsely denying access**: `[farmId]/settings/page.tsx` checked the global `user_profiles.role` instead of the per-farm `farm_members.role` used everywhere else (`layout.tsx`'s `effectiveRole` pattern); an admin-on-this-farm user whose global profile defaulted to `worker` was wrongly redirected away from Settings — exactly where the GPS lat/lng fields live. Now fetches `farm_members` for the current farm and only redirects when the effective per-farm role is `worker`. (2) **"+ Create new farm" silently no-op'd**: `FarmSwitcher.tsx`'s "Create new farm" link pointed at `/farms`, but `app/farms/page.tsx` unconditionally redirected any single-farm user straight back to their dashboard, making the `FarmPicker`/`CreateFarmWizard` unreachable. `/farms` now accepts a `?new=1` param (added by the FarmSwitcher link) that skips the single-farm auto-redirect and auto-opens the wizard; normal login flow is unaffected. (3) **No way to delete a farm**: the `deleteFarm()` server action existed but had zero UI call sites. Added a "Danger Zone" section to Settings → Weather & AI (admin-only tab) with type-the-farm-name-to-confirm delete, wired to the existing action, redirecting to `/farms` on success. Lint + `tsc --noEmit` clean. |
| 2026-07-03 | Fixed Team tab role management (same root cause as the Settings bug above) — `updateUserRole`/`createWorker` in `settings/actions.ts` authorized against the global `user_profiles.role` and only ever wrote to `user_profiles.role`, never touching `farm_members`. Since per-farm role is authoritative everywhere else, a farm admin whose global profile defaulted to `worker` could never change anyone's role (including their own), and `createWorker` never inserted a `farm_members` row for the new account — new workers/supervisors were created with no farm membership at all and would never appear in that farm's Team list. Also fixed a fragile `isSelf` check in `TeamTab` (compared `full_name`/`phone` instead of user id) that could misfire and render an editable role dropdown for the current user's own row. Both actions now take `farmId`, check the caller's `farm_members.role`, and (for `updateUserRole`) update `farm_members.role` directly; `createWorker` now also inserts the new user into `farm_members` for the current farm. `settings/page.tsx` now builds the Team list's roles from `farm_members` instead of `user_profiles`. Lint + `tsc --noEmit` clean. |
| 2026-07-03 | Fixed "New Block" option vanishing after the first block (third instance of the same global-vs-per-farm role bug) — `[farmId]/blocks/page.tsx` passed `userRole` from the global `user_profiles.role` instead of the per-farm `farm_members.role`. The header "New Block" button is hidden whenever `userRole === "worker"`, so a farm admin whose global profile defaulted to `worker` never saw it; the only way to create a block at all was an unguarded empty-state button shown solely when `blocks.length === 0`, which disappears the instant the first block exists — leaving no visible way to add a second one. Fixed `blocks/page.tsx` to compute `effectiveRole` from `farm_members` (same pattern as `layout.tsx`/`settings/page.tsx`), and made the empty-state button in `BlocksPage.tsx` respect the same `userRole !== "worker"` gate as the header button and the edit/delete controls, instead of being unconditionally shown. Lint + `tsc --noEmit` clean. |
| 2026-07-04 | Team tab — Added support for attaching an already-registered user to a new farm. Previously `createWorker` (`settings/actions.ts`) always called `auth.admin.createUser()`, so submitting the "Add Team Member" form with an email that already had an account on another farm always failed with "A user with this email address has already been registered," with no path to actually add them. `createWorker` now looks the email up first (new `findAuthUserByEmail` helper, paginating `auth.admin.listUsers()` since the SDK has no email filter); if a match exists it skips account creation and `user_profiles` entirely and just inserts a `farm_members` row for the current farm (returning a friendly error if they're already a member of this farm), otherwise it falls back to the original create-new-user flow unchanged. Added a short hint under the password field in `SettingsForms.tsx` explaining the behavior. Lint + `tsc --noEmit` clean. |
| 2026-07-04 | Team tab — Fixed newly-added member not appearing in the "Team Roles & Access Control" table until a manual page reload. Verified against the live DB that `createWorker`'s insert was correct (the new `farm_members`/`user_profiles` rows were present), so the bug was purely client-side: `onNewWorkerSubmit` reset the form on success but never told the Next.js router to refetch the server-rendered `allUsers` list. Added `router.refresh()` after a successful add in `TeamTab` (`SettingsForms.tsx`). Lint + `tsc --noEmit` clean. |
| 2026-07-04 | Security fix — Scoped Recommendations to farms (three compounding bugs). (1) **`recommendations` table had no `farm_id` column at all** — `getRecommendations()` returned every recommendation for every farm in the database to whichever farm's page loaded it. Added migration `supabase/migrations/20260704000000_scope_recommendations_to_farms.sql`: adds `farm_id`, backfills from `block_id -> blocks.farm_id` (verified all 16 production rows cleanly matched a real block), enables RLS with farm-scoped read/write policies. **Requires running this migration manually in the Supabase SQL editor.** (2) **The Recommendations page's client component (`RecommendationsClient.tsx`) imported its accept/skip/edit/generate actions from a leftover legacy file** (`app/(dashboard)/recommendations/actions.ts`) instead of the farm-aware one at `app/[farmId]/(dashboard)/recommendations/actions.ts` — the `farmId` prop was received but unused. Consolidated all logic (including `generateAIRecommendations`/`generateMockRecommendations`, which only existed in the legacy file) into the farm-aware `actions.ts`, threaded `farmId` through every call, and deleted the dead legacy file. Also fixed the page's worker-redirect check to use the per-farm `farm_members.role` (same fix pattern as Settings/Blocks) instead of only the global `user_profiles.role`. (3) **The weekly AI cron job** (`src/trigger/recommendations.ts`, runs Mondays 04:00 UTC) built one combined AI context across every block in every farm and sent it as a single OpenAI call — cross-farm data mixed into one prompt, no `farm_id` on inserted rows. Rewrote it to loop per farm, calling `buildAllBlockContexts(admin, farmId)` and generating/inserting recommendations one farm at a time. `npm run build` + `tsc --noEmit` + lint clean. |
| 2026-07-03 | **Security fix — Inventory (assets/consumables) had zero farm scoping.** `assets`, `consumables`, `asset_maintenance_log`, and `consumable_usage_log` had no `farm_id` column at all; `getAssets`/`getConsumables`/`createAsset`/`createConsumable` queried and inserted with no farm filter whatsoever, so every farm on the entire live site shared one global inventory list — any account could see (and silently write into) every other account's assets and consumables. Root cause was compounded by dead code: `InventoryPage.tsx` imported its mutation actions from a stale pre-multi-farm route group (`app/(dashboard)/inventory/actions.ts`, now deleted) that didn't even accept a `farmId` parameter, while the farm-scoped route (`app/[farmId]/(dashboard)/inventory/actions.ts`) had a `farmId?` param that was only used for `revalidatePath`, never for filtering. Fix: (1) new migration `20260703000000_scope_inventory_to_farms.sql` — deletes all existing inventory rows (per product decision, since they can't be reliably attributed to a specific farm) and adds a required `farm_id` column + RLS policies (scoped directly on `assets`/`consumables`, transitively via join for the two log tables) to all four tables, matching the existing `sensors`/`calendar_event_materials` RLS pattern; (2) `inventory/actions.ts` — `getAssets`/`getConsumables` now filter `.eq('farm_id', farmId)`, `createAsset`/`createConsumable` now write `farm_id` on insert, maintenance/usage log reads now scope via the farm-filtered asset/consumable ID list instead of fetching every row in the table; (3) `InventoryPage.tsx` now imports all inventory actions from the farm-scoped file and passes `farmId` through to every mutation; (4) `inventory/page.tsx` and `blocks/page.tsx`'s sibling `calendar/page.tsx` also had the global-role bug (4th/5th instance) — both now compute `effectiveRole` from `farm_members`. **Action required**: run `supabase/migrations/20260703000000_scope_inventory_to_farms.sql` against the production Supabase project — this permanently deletes all existing asset/consumable rows across all farms, per an explicit decision that the pre-existing data couldn't be safely attributed to any one farm. Lint + `tsc --noEmit` clean. |
| 2026-07-05 | Team tab — Added a copyable invite message for brand-new team members. Previously, after creating a new (not-yet-registered) worker/supervisor, `createWorker` just returned a generic "Successfully created" banner and immediately reset the form — the admin-typed temporary password disappeared and was never shown again, so admins had to write it down separately to pass along via WhatsApp/email/etc. `createWorker` (`settings/actions.ts`) now returns an `isNewUser` flag so the client can distinguish "brand-new account created" from "existing user added to this farm" without string-matching. `TeamTab` (`SettingsForms.tsx`) captures the email/password from the submitted `FormData` on the new-user path and shows a card with the farm name, email, temporary password, and login URL (`NEXT_PUBLIC_APP_URL`), plus a "Copy invite message" button (reusing the existing `CopyButton` component) that copies a ready-to-paste message for any communication channel. Never shown for the "added an already-registered user" path, since those users keep their own password. Lint + `tsc --noEmit` clean. |
| 2026-07-05 | Team tab — Added the ability for admins to remove a team member from a farm. Previously the only member-management actions were adding members and changing roles — there was no way to revoke someone's access to a farm. New `removeMember(farmId, userId)` server action (`settings/actions.ts`), mirroring `updateUserRole`'s auth pattern: caller must be an admin on the farm, cannot remove themselves, and cannot remove the farm's only remaining admin (guards against leaving a farm with no admin). Deletes the `farm_members` row only — the user's Supabase Auth account, `user_profiles` row, and any other farm memberships are untouched (confirmed no other table has a foreign key into `farm_members`). `TeamTab`'s roster table now has a "Remove" action per row (hidden on the current admin's own row) with an inline "Confirm remove? / Cancel" swap instead of a popup dialog, consistent with the tab's existing lightweight inline-status pattern. Lint + `tsc --noEmit` clean. |
| 2026-07-23 | Blocks page swipe gestures — `BlocksPage.tsx` now supports left/right touch swipe to cycle the selected block, mirroring Calendar's inline `useRef` + `onTouchStart`/`onTouchEnd` pattern (50px threshold, wraps around the block list). Closes the last 🟡 partial item in Mobile Optimisation. `tsc --noEmit` + lint clean. |
| 2026-07-11 | Resend transactional email — replaces the copy/paste-only invite flow and push-only alert flow with real emails sent from `hello@rootloot.ai` (domain purchased via GoDaddy; sending goes through Resend's free tier since GoDaddy's own email offering is a trial/forwarding only, not a real programmatic sender). Added `resend` dependency and new `utils/email.ts` (mirrors `utils/push.ts`'s conventions: module-level client init, no-ops gracefully if `RESEND_API_KEY` is unset) exporting `sendEmail`, `sendInviteEmail`, `sendAlertEmail`. `createWorker` (`settings/actions.ts`) now auto-sends an invite email to brand-new team members after the `farm_members` insert succeeds (non-blocking; on failure returns a `warning` so the UI still shows "Copy invite message" as a fallback — that button is unchanged). `/api/ingest/alert` now also emails farm admins/supervisors (in addition to the existing push notification) for `warning`/`critical` severity alerts, via a new `notifyAdminsByEmail` helper that resolves `farm_members` (admin/supervisor) `user_id`s to emails through `auth.admin.getUserById`. `.env.local.example` documents new `RESEND_API_KEY`/`EMAIL_FROM` vars. `npm run lint` + `npm run build` clean. **Action required — DNS not yet configured**: `rootloot.ai`'s nameservers point at Netlify DNS (not GoDaddy's own DNS), and the Netlify project is currently suspended for exceeding its monthly usage allowance; once it resets, add Resend's DKIM/SPF/DMARC records in Netlify's DNS panel, verify the domain in Resend, and set `RESEND_API_KEY`/`EMAIL_FROM` in the production environment before either email path will actually send. |
| 2026-08-16 | **Super Admin area (new, platform-level).** NutJob had no billing infrastructure and no cross-farm admin view at all; `Requirements.md` already specified a not-yet-implemented B2B organisation/billing design, so this implements that design rather than inventing a new one. (1) **`super_admin` role** — `20260817000000_add_super_admin_role.sql` adds the enum value to `user_profiles.role` (separate from the per-farm `farm_members.role`); granted only via manual SQL, no self-service path. (2) **Route guard** — `app/(superadmin)/admin/layout.tsx`, a layout-level check (matches the `[farmId]/(dashboard)/layout.tsx` precedent rather than adding a DB round-trip to global middleware); `app/components/admin/AdminNav.tsx` is new dark-sidebar chrome, intentionally not reusing the farm-scoped `Sidebar`. (3) **Organization/billing data model** — `20260817100000_create_organizations.sql`: `organizations` (billing entity: Stripe customer/subscription, `subscription_status`, `farm_seats`) + `organization_members` (owner/admin/member), `farms.organization_id` FK (nullable — existing farms are not backfilled/guessed into an org). `app/actions/organizations.ts`'s `getOrCreateOrganizationForUser()` silently creates a 1-person org the first time a user without one creates a farm. `app/actions/farms.ts`'s `createFarm()` now gates on org seat count instead of the old flat 3-farm cap (`farm_seats` defaults to 3 specifically to avoid regressing existing multi-farm users — stricter than the spec's trial-of-1, revisit once seat purchasing is wired into a real onboarding flow). (4) **Stripe integration** — added `stripe` dependency, `utils/stripe.ts` client, `createCheckoutSession()` (subscription-mode Checkout, owner-only); `app/api/webhooks/stripe/route.ts` verifies the signature (unauthenticated by normal session logic, same exemption pattern as `/api/cron/*`'s `CRON_SECRET`) and syncs `subscription_status`/`farm_seats` on `checkout.session.completed`/`customer.subscription.updated`/`.deleted`/`invoice.payment_failed`, caching paid invoices into `stripe_invoices` (`20260817200000_create_stripe_invoices_cache.sql` — no client-readable RLS policy at all; reads only via the service-role client behind `requireSuperAdmin()`). (5) **Super Admin UI** — `/admin/overview` (every farm + owner + org plan + member/block counts), `/admin/subscribers` (every organization + plan/seats/member/farm counts), `/admin/subscribers/[orgId]` (members, farms with a jump-in link to that farm's dashboard, and Stripe payment history with links to hosted invoices). All reads go through `requireSuperAdmin()` (`app/(superadmin)/admin/actions.ts`) using the service-role client, since RLS otherwise blocks cross-tenant reads. **Deferred, not built here**: the org-owner-facing Settings → Billing tab UI that would actually let an org owner call `createCheckoutSession` (the action exists as shared infra, but there's no self-service entry point to it yet), and past-due grace-period enforcement on farm-scoped write actions. **Action required**: run all three new migrations in the Supabase SQL editor in order; set `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET`/`STRIPE_PRICE_ID_FARM_SEAT` in the environment (documented in `.env.local.example`) and register the webhook endpoint in the Stripe dashboard before Stripe integration will actually function; manually promote a test user to `super_admin` via SQL to access `/admin`. `tsc --noEmit` + lint clean; production `next build` not verified end-to-end in this environment (blocked by an unrelated pre-existing Google Fonts network-fetch failure in `app/layout.tsx`, not caused by this change). |
| 2026-08-11 | RAG-grounded AI recommendations — the AI Agronomist's advice previously came entirely from the LLM's own unverified training knowledge; now each recommendation can cite a real, trusted source. Added `pgvector` extension + `knowledge_base_chunks` table (migration `20260812000000_add_rag_knowledge_base.sql`) and a `sources` JSONB column on `recommendations` (`20260812000001_add_sources_to_recommendations.sql`), plus a `match_knowledge_base` cosine-similarity RPC. New one-off developer script `scripts/ingest-knowledge-base.ts` (`npm run ingest:kb -- --crop=<crop_type>`) chunks and embeds manually-downloaded extension-manual PDFs (starting corpus: UC ANR's Almond Production Manual for `--crop=almond`); each chunk is tagged with the crop it applies to (or left crop-agnostic) so a farm growing a different crop never gets almond-specific citations — adding a new crop's corpus later needs no code changes, just another ingestion run. Consolidated the previously-duplicated generation logic from `src/trigger/recommendations.ts` (weekly cron) and `actions.ts` (`generateAIRecommendations`, on-demand) into a new shared `utils/generate-recommendations.ts`, which now also retrieves crop-matched reference chunks per block and injects them into the prompt before the LLM call. `AI_SYSTEM_PROMPT` (`utils/build-block-context.ts`) updated to require the model to cite its source or leave `sources` empty rather than fabricate one, and de-scoped from "specialising in almond farming" to per-block crop awareness. Citations now render on `RecommendationCard.tsx` (compact badge) and in the edit modal (`RecommendationsClient.tsx`, full list under a new "Sources" section) — added `aiSources` i18n key to all three locale files. **Action required**: run both new migrations in the Supabase SQL editor, then run `npm run ingest:kb -- --crop=almond` after downloading UC ANR Pub 3364 into `./knowledge-base-source/almond/` (gitignored) — until that's done, retrieval returns no chunks and recommendations generate exactly as before, just with an empty `sources` array. `tsc --noEmit` + lint clean. |
| 2026-08-30 | **VPS migration — Phase 1 (containerization) complete; server-side phases pending.** Migrating off Netlify + Supabase Cloud onto the Hostinger VPS documented in `INFRA.md`/`SETUP.md` (Coolify 4.3.14, Ubuntu 26.04). Decision: **self-host the full Supabase stack** rather than move to plain Postgres — the app has ~220 PostgREST `.from()` calls, ~47 `supabase.auth.*` calls, 7 GoTrue admin-API calls and 18 RLS policies keyed on `auth.uid()`, so self-hosting is a three-env-var change where plain Postgres would be a multi-week rewrite. **Built this pass:** new `Dockerfile` (multi-stage `node:20-alpine`, matching the `NODE_VERSION` that was pinned only in `netlify.toml` — the repo's sole record of the intended runtime) and `.dockerignore`; `next.config.ts` gains `output: 'standalone'` and the CSP `connect-src` no longer hardcodes `https://*.supabase.co` but is **derived from `NEXT_PUBLIC_SUPABASE_URL`**, so cloud and self-hosted both work with no edit (verified in `routes-manifest.json`). **Bug found and fixed:** `utils/stripe.ts` threw at *module load*, and Next 16's "collect page data" step imports `app/api/webhooks/stripe/route.ts` at build time — so `next build` failed outright without a Stripe key. Invisible on Netlify (its build env has the key), fatal in a container. Fixed by deferring construction behind a `Proxy` (mirrors the lazy pattern in `utils/email.ts`); all four call sites unchanged, the build now needs no Stripe secret, and a genuinely missing key still throws loudly on first use. The rejected alternative — passing `STRIPE_SECRET_KEY` as a Docker build `ARG` — would have baked a live payment secret into an image layer. `npm run build` now clean end-to-end (28 routes, standalone `server.js` emitted); note this also clears the "production build not verified" caveat left on the 2026-08-16 entry. Docs corrected: `CLAUDE.md`/`PROGRESS.md` tech-stack tables claimed **Netlify AI Gateway**, which no code has ever read — every LLM call goes direct to OpenRouter; `.env.local.example` loses the `NETLIFY_AI_GATEWAY_URL` line and gains `NEXT_PUBLIC_OPENROUTER_CONFIGURED`. **Two findings that change the endgame — read before decommissioning anything.** (1) **`rootloot.ai`'s DNS is hosted by Netlify** (nameservers `dns1–4.p07.nsone.net` = NS1/Netlify DNS, confirmed live). Deleting the Netlify account therefore takes the whole domain down, including MX and the Resend DKIM/SPF/DMARC records — DNS must be moved to Cloudflare *first*, while Netlify still works and the old records are still readable. Combined with the 2026-07-11 note that the Netlify project was once suspended for exceeding its usage allowance, a billing lapse there is currently a total domain outage. (2) The core schema is **not in `supabase/migrations/`** — only 11 `CREATE TABLE`s exist there, while `farms`, `blocks`, `farm_members`, `user_profiles`, `calendar_events`, `recommendations` and ~10 more were created by hand in the Supabase SQL editor. A `pg_dump` of the live project is the only source of truth and is a hard prerequisite for the database phase; it will be committed as a baseline migration to close that gap permanently. **Action required / still pending:** add `app-vps.rootloot.ai` + `supabase.rootloot.ai` A records → `187.53.128.214` (in Netlify's DNS panel, for now); add 4 GB swap to the VPS before any on-box build (7.7 GB, currently 0 swap — `next build` is the usual OOM casualty); capture the Supabase dumps; deploy the Coolify Supabase stack and restore; recreate the `lab-reports` storage bucket and the Google OAuth provider by hand (neither is in any dump); re-point the Stripe webhook and any sensor devices POSTing to `/api/ingest/*`. Note `.env.local` on the dev machine has **empty** Stripe values and no `RESEND_API_KEY`/`EMAIL_FROM` at all — those live only in Netlify's UI and must be collected from there before cutover. Users will have to log in once more after cutover (new `JWT_SECRET` invalidates existing cookies; bcrypt password hashes survive the dump). |
| 2026-08-30 | **VPS migration — Phases 0–3 substantially complete (paused here).** Continues the entry above. **Done this session:** (1) **DNS** — `app-vps.rootloot.ai` and `supabase.rootloot.ai` A records → `187.53.128.214`, added in Netlify's DNS panel, verified resolving authoritatively via NS1. (2) **Self-hosted Supabase deployed** on the VPS via Coolify as an *isolated second stack* (service `supabase-rsq9qfpoarm7fm1uz8qogddb`, Postgres 17.6). Kong is the only container with a public domain (`https://supabase.rootloot.ai`, valid Let's Encrypt cert); Studio deliberately has none and is tunnel-only. Verified from outside: `/rest/v1/` and `/auth/v1/health` return 401 (Kong routing correctly), Studio paths 401, Postgres 5432 filtered, only 22/80/443 open. (3) **Database captured and restored** — see the corrections below. **Two significant corrections to earlier assumptions:** the live database has **71 RLS policies, not 18** (the repo's migrations only ever contained 18; the rest were written by hand in the SQL editor), and there **is** a trigger `on_auth_user_created` on `auth.users` that creates the `user_profiles` row for new signups — earlier analysis concluded there were none and that this was app-code-only. Both are now verified present on the new stack. **New file `supabase/migrations/00000000000000_baseline_public_schema.sql`** — the `pg_dump --schema-only --schema=public` of the cloud project, committed to close a real gap: the repo's migrations held 11 `CREATE TABLE`s while the live database has 26 tables, 71 policies and 5 views, with the remainder existing only inside the running cloud database. Its header documents what it deliberately omits (the `auth`/`storage` schemas, including `on_auth_user_created`). **Restore status — public schema 100%, one auth gap:** every public table matches the source exactly (farms 6, blocks 15, farm_members 7, user_profiles 6, recommendations 34, knowledge_base_chunks 184, activity_log 5, soil_water_readings 3; 71 policies, 26 RLS tables, 5 views, pgvector 0.8.0 + `match_knowledge_base` present, `auth.users` 6). **`auth.identities` initially restored 0 of 6** — the dump's COPY for it ran before `auth.users` was populated, so `identities_user_id_fkey` rejected all 6 rows; re-running just that COPY block afterwards loaded all 6. **Now verified complete: `auth.users` 6, `auth.identities` 6.** The restore is finished and every figure matches the source exactly. Of the 241 restore errors, **0 touched the public schema**; the rest were "already exists" conflicts from restoring over a GoTrue-initialised auth schema, which is expected. **Two process notes worth carrying forward:** the custom-format dump was unusable (`unsupported version (1.16) in file header` — pg_dump 17.11 writes a newer archive revision than the container's pg_restore 17.6 reads), so plain SQL was used instead and is the better default here; and Supabase Cloud's *direct* connection is IPv6-only on the free tier and unreachable from the workstation, so the capture went through the **session pooler** on `aws-1-us-east-1.pooler.supabase.com:5432` (the transaction pooler on 6543 will not work with pg_dump). **Action required before cutover:** recreate the `lab-reports` storage bucket (private — it holds **0 objects**, so nothing to migrate); reconfigure Google OAuth (2 of 6 users use it) and add `https://supabase.rootloot.ai/auth/v1/callback` to the Google console; deploy the app to `app-vps.rootloot.ai`; collect `STRIPE_*` and `RESEND_*` values from Netlify's UI (they exist nowhere else — `.env.local` has empty Stripe values and no Resend keys); re-point the Stripe webhook and any sensor devices. **Security:** the Supabase database password was echoed in plain text during a failed connection attempt and must be rotated (safe to do — the app uses the anon/service-role keys, not the DB password). `/tmp/nutjob-full.sql` and `/tmp/nutjob-full.dump` on the VPS contain `auth.users` password hashes and should be deleted once the identities fix has run. **Blocker for decommissioning Netlify:** `rootloot.ai`'s nameservers are Netlify's (`dns1–4.p07.nsone.net`), so DNS must move to Cloudflare *before* the Netlify account is deleted or the whole domain goes dark. The zone is only 4 records, so this is easy — and the record list shows Resend's DKIM/SPF/DMARC were never added, meaning `hello@rootloot.ai` is almost certainly not delivering today (a pre-existing bug, not caused by this migration). |
| 2026-08-30 | **VPS migration — Phase 4 complete: the app is deployed and serving at `app-vps.rootloot.ai`.** Continues the entries above. Coolify Application created against `gharaybehm/NutJob@main`, build pack Dockerfile, port 3000, domain `https://app-vps.rootloot.ai` with a Let's Encrypt certificate. Configured via Coolify's REST API rather than the dashboard — the SSH-tunnelled UI was taking ~13s per page, and setting 18 variables by hand (five needing a build-time flag) through that was both slow and a good way to mistype a Supabase key. **13 environment variables set**, five flagged build-time; the Supabase anon and service-role keys were read directly out of the running stack via the API, so there was no transcription step for the two values most likely to break things silently. Note Coolify's API creates *two* rows per POST to `/envs`, so a dedupe pass was needed — 26 rows became 13. **Verified from outside:** HTTP 200 with a valid cert, `<title>RootLoot Farm Management</title>`, `/` → `/login` redirect working, CSP `connect-src` correctly reading `https://supabase.rootloot.ai wss://supabase.rootloot.ai` (proving the derived-origin change in `next.config.ts` survives a container build), no reference to the old cloud project in the served bundles, and `X-Frame-Options: DENY` intact. **Three build failures preceded this, all worth recording because they are generic to containerizing this app.** (1) **`npm ci` EUSAGE** — `package-lock.json` was missing nested `@tailwindcss/oxide-wasm32-wasi` entries. (2) **npm major skew** — regenerating the lock did not help: the lock is authored by npm 11 on the dev machine while `node:20-alpine` ships npm 10, and the two disagree about a real peer conflict (`@swc/helpers` pinned to 0.5.15 by one dependency, `>=0.5.17` peer-required by another). npm 11 accepts the tree; npm 10 resolves the peer to 0.5.23, finds it absent, and aborts. Fixed by moving the image to `node:24-alpine` so prod matches the lock's author — matching dev to prod is worth more than matching the Netlify `NODE_VERSION` being abandoned. (3) **Clients constructed at module load** — Next's "collect page data" imports every route at build time, so `next build` demanded live credentials. This is the same bug already fixed for Stripe before deployment, recurring in four more places. New `utils/openrouter.ts` replaces four identical `new OpenAI({...})` blocks with one lazily-constructed client (the old `apiKey: ... || ""` also obscured the failure: an empty string is not a missing key, so the SDK said "Missing credentials" rather than naming the variable), and `utils/push.ts` no longer calls `webPush.setVapidDetails()` at import — `VAPID_PRIVATE_KEY` is runtime-only and must never be a build arg. `utils/supabase/admin.ts` was checked and needed no change. Verified by building with `.env.local` moved aside and no secrets in the environment — the same condition the container builds under; `tsc --noEmit` clean. **Peak build memory 5.8 GB of 7.7 GB with ~2.1 GB still available**, so the box handled `next build` alongside two Supabase stacks without touching swap meaningfully. **Not configured, deliberately:** Stripe and Resend. Confirmed with the operator that neither was ever configured on Netlify either, so this is not a regression — `.env.local` has empty Stripe values, and the 2026-08-16 entry already flagged the Stripe keys as outstanding. Stripe pages will error when touched; email is a silent no-op (`utils/email.ts` returns early without a key). **Still open before cutover:** reconfigure Google OAuth on the new stack and add `https://supabase.rootloot.ai/auth/v1/callback` to the Google console (2 of 6 users sign in that way); recreate the `lab-reports` storage bucket (private, 0 objects); end-to-end testing, especially **multi-tenant isolation across the 71 restored RLS policies**; move DNS to Cloudflare before Netlify can be retired; rotate the exposed Supabase Cloud database password; revoke the Coolify API token created for this session. Everyone will need to sign in again — the new stack has a different `JWT_SECRET`, so existing cookies are unverifiable, though passwords work since the bcrypt hashes came across in the restore. |
| 2026-08-31 | **VPS migration — storage and Google OAuth done; app is feature-complete on the VPS.** (1) **Confirmed the two Supabase stacks are genuinely separate**, after a query about whether NutJob shares a database with the Rabita MLS project on the same box. They do not: distinct containers and host PIDs, **distinct Docker volumes** (`rsq9qfpoarm…_supabase-db-data` vs `lnvzqyyiyr…_supabase-db-data`), 26 farm tables vs 22 unrelated real-estate tables, no `farms` table in Rabita, and **different JWT secrets** — so a token from one is meaningless to the other. What *is* shared is the host and the Coolify control plane: a blast-radius and resource concern, not a data-isolation one. (2) **`lab-reports` storage bucket recreated** (private) and, more importantly, **a pre-existing bug fixed**: `storage.objects` had RLS enabled but **zero policies** — 81 policies existed on `public.*`, none on `storage.*`. RLS enabled with no policies denies everything, and `app/actions/soilTests.ts` uploads with the *user* client, so **lab-report uploads had never worked**; `storage.objects` held 0 rows, and applying the fix reported 0 pre-existing policies. Both confirm it. Not caused by the migration — a faithful move would have reproduced the broken behaviour. New migration `20260831000000_lab_reports_storage_policies.sql`: SELECT is farm-scoped indirectly (an object is readable only when a `soil_water_readings` row references it, and that subquery is itself subject to that table's RLS), while INSERT is authenticated-only because the upload path is flat (`reports/<uuid>.<ext>`), carries no farm id, and the reading row does not exist yet at upload time. Exposure is bounded: an orphan object lands on an unguessable UUID path and stays unreadable unless linked from a row the user owns. **Follow-up worth doing while the bucket is still empty:** move to `<farmId>/reports/<uuid>.<ext>` and scope INSERT on the path too — needs `farmId` threaded from `BlocksPage` → `LogTestResultModal` → form → action, plus a decision for "Whole Farm" tests where no block implies a farm. (3) **Google OAuth configured** on the new stack (`GOTRUE_EXTERNAL_GOOGLE_*`, redirect `https://supabase.rootloot.ai/auth/v1/callback`), service restarted, `/auth/v1/settings` now reports `google: true`, and **sign-in verified working end to end by the operator**. The Google client secret was exposed in the session transcript during entry and was **rotated** before use — note the old client secret was replaced on the shared OAuth client, so the still-live Netlify deployment now uses the new secret too. **Remaining before Netlify can be retired:** rotate the Supabase Cloud database password (exposed earlier); revoke the Coolify API token created for this work (needed until cutover completes); tenant-to-tenant isolation test with two accounts in different farms — anonymous access is already proven blocked (anon key returns 0 rows from `farms`/`blocks`/`user_profiles` while the service-role key returns 6/15/6 from the same endpoints, so the zeros are RLS refusing rather than empty tables); move DNS off Netlify to Cloudflare; point `www.rootloot.ai` at the VPS, update `NEXT_PUBLIC_APP_URL` **and rebuild** (it is a build-time variable) plus `GOTRUE_SITE_URL`; then set up backups, which are now genuinely urgent since production data will live on one box. |
| 2026-08-31 | **CUTOVER COMPLETE — `www.rootloot.ai` now serves from the VPS.** Netlify no longer receives production traffic. Sequence: added `www.rootloot.ai` alongside `app-vps.rootloot.ai` on the Coolify application (keeping the test URL as a fallback), set `NEXT_PUBLIC_APP_URL` → `https://www.rootloot.ai` and **rebuilt** (it is a build-time variable, so a restart would not have been enough), set `GOTRUE_SITE_URL` → `https://www.rootloot.ai` and `ADDITIONAL_REDIRECT_URLS` to cover both hosts, then switched the `www` record in Netlify's DNS from a NETLIFY record to `A → 187.53.128.214`. Verified: HTTP 200, valid Let's Encrypt certificate (`CN=www.rootloot.ai`, expires 2026-11-29), `X-Powered-By: Next.js` confirming the VPS rather than Netlify, `/` → `/login` redirect intact, CSP `connect-src` pointing at `https://supabase.rootloot.ai`, and **email + Google login both confirmed working on the production domain by the operator**. **A ~10 minute certificate outage occurred during cutover, caused by ordering.** The domain was added to Coolify and deployed *before* DNS pointed at the box, so Let's Encrypt validated the HTTP-01 challenge against Netlify, failed, and Traefik fell back to `TRAEFIK DEFAULT CERT` — visitors saw an untrusted-certificate warning while the app itself was healthy. Fixed by redeploying once DNS had propagated, which retriggers issuance. **Correct order for the apex record, still to be moved: change DNS first, then add the domain in Coolify (or redeploy after DNS lands).** The apex `rootloot.ai` still points at Netlify and redirects to `www`, so it works, with one extra hop. **Credential cleanup outstanding.** Four secrets were exposed by a masking bug in this session's own output (values under 50 characters were printed rather than masked by key name): `VAPID_PRIVATE_KEY` (rotating it invalidates existing push subscriptions), `TRIGGER_SECRET_KEY`, `CRON_SECRET` and `TREFLE_API_KEY`. Also still open: the Supabase Cloud database password and the Google client secret were both exposed earlier — the Google one was rotated before use, and note the replacement is shared with the still-deployed Netlify site since it is the same OAuth client. The Coolify API token remains needed until all remaining work is finished, then should be revoked. **Next, in priority order:** set up backups — now genuinely urgent, since production data lives on a single box with no snapshots (`INFRA.md` §7 and the top item in `SECURITY.md`); move the apex record; rotate the exposed secrets; migrate DNS from Netlify to Cloudflare (4 records, and the moment to finally add Resend's DKIM/SPF/DMARC, which were never configured); keep Netlify deployed but dark for roughly a week as the rollback path, then delete it — **only after DNS has moved**, since Netlify is still the domain's nameserver. |
| 2026-08-31 | **Backups set up and verified — the top item in `SECURITY.md` is now closed.** Coolify's own backup feature covers *managed database* resources; NutJob's Postgres lives inside a service, so `/api/v1/databases` is empty and that feature does not apply. Installed a systemd timer instead (`nutjob-backup.timer`, 02:30 UTC daily, `Persistent=true`, deliberately just before the box's 03:00 unattended-upgrades reboot window so a dump always exists from immediately before a kernel restart). `/usr/local/sbin/nutjob-backup.sh` dumps **all schemas** — `auth.users` included, since a public-only dump restores a database nobody can log into — gzips to `/var/backups/nutjob`, writes a SHA-256, and keeps 7 daily + 4 weekly. It **refuses to keep a bad dump**: it rejects output under 100 KB, verifies gzip integrity, and greps for `CREATE TABLE public.farms`, `COPY auth.users` and `CREATE POLICY` before promoting the `.partial` file. A dump that runs but captures nothing is the usual way people discover an empty backup at restore time. **Restore tested, not assumed** (`INFRA.md` §7: "an untested backup is a guess"): the newest dump was restored into a throwaway `restore_test` database inside the same container, with production never written to. Result — **0 errors**, and row counts identical to live: farms 6, blocks 15, farm_members 7, user_profiles 6, recommendations 34, knowledge_base_chunks 184, `auth.users` 6, and **73 policies** (the 71 public plus the 2 new storage ones). **Off-box copy closed the remaining gap:** `/var/backups` is root-only, so each run now also stages the newest dump to `~deploy/nutjob-backups` (mode 600, deploy-owned — it holds password hashes and this box also hosts another project), making it pullable without sudo. One copy was pulled to `C:\Users\mhrg7\Documents\nutjob-backups\` and independently verified there: SHA-256 matches the VPS-computed hash, gzip intact, and all critical objects present including 73 `CREATE POLICY` statements. **Operator also took a manual Hostinger snapshot and scheduled a free weekly one**; Hostinger's automated *daily* backups are a $6/mo add-on and were declined for now. Resulting posture: nightly SQL dumps for logical mistakes, weekly VM snapshots for machine loss, and a manual off-box copy for provider loss. **The off-box pull is manual** — `scp vps:~/nutjob-backups/* ~/nutjob-backups/` — deliberately not automated, since a Windows Scheduled Task would need the SSH key passphrase-free. |
